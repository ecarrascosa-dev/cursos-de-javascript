const add = function(a,b) {
    return a+  b
}

const subtratc = function(a, b) {
    return a - b
}

const multiply = (x, y) => x * y

const divide = (x, y) => x / y

console.log(add(10, 20))
console.log(subtratc(10, 20))
console.log(multiply(10, 20))
console.log(divide(5, 25))