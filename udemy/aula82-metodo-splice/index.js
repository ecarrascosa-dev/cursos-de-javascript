const nomes = ['maria', 'joão', 'eduardo', 'gabriel', 'julia'];

// nomes.splice(InputDeviceInfo, delete, elem1, elem2, elem3);
// pop

const removidos = nomes.splice(-2, 2);
console.log(nomes, removidos);